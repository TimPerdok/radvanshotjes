export type Theme = {
    colors: {
        primary: string,
        secondary: string,
        primaryText: string,
        secondaryText: string,
    },
    breakpoints: {
        xs: number,
        sm: number,
        md: number,
        lg: number,
        xl: number,
    }
}
