import { Theme } from '../types/types';

export default {
    colors: {
        primary: '#86CB92',
        secondary: '#0d2140',
        primaryText: "white",
        secondaryText: "grey",
        
    }
} as Theme;